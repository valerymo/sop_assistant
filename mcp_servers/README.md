# MCP Servers

This directory contains Model Context Protocol (MCP) tool servers used by the SOP Assistant.

Each server exposes tools that can be called by the assistant.

Servers:

- sop_server — access SOP documents and RAG search
- kubernetes_server — Kubernetes debugging tools
- git_server — repository analysis tools
- observability_server — metrics/log analysis tools
